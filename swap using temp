#include <stdio.h>
int main() {
    int a,b,temp;
    printf("enter your values:- ");
    scanf("%d" "%d",&a,&b);
    printf("Before swapping a=%d,b=%d",a,b);
    temp=b;
    b=a;
    a=temp;
    printf("After swapping a=%d,b=%d",a,b);
    return 0;
}
